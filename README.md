# Being A Mom

This repository contains the current Being A Mom app:

- A Flutter mobile client in `being_a_mom/`
- A Django backend API in `beingamom/` and `chat/`

The old Django template-based web UI and generated build/cache files were removed so the repo only keeps the active app code and supporting files.
